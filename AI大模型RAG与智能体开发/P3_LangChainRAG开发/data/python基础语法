### 4.1 列表（list）
 - **特点**：有序、可变、可重复、可存放不同类型元素
 - **创建**：`lst = [1, 2, 3]` 或 `list()`
 - **索引**：正向 `0~n-1`，反向 `-1~-n`
 - **切片**：`lst[start:stop:step]` — 截取子列表
 - **常用方法**：
   - `append(x)`：末尾追加
   - `insert(i, x)`：指定位置插入
   - `remove(x)`：删除第一个匹配项
   - `pop(i)`：删除并返回指定位置元素
   - `sort()` / `reverse()`：排序 / 反转
   - `index(x)`：查找元素索引
   - `len(lst)`：获取长度
 - **列表解包**：`a, b, c = [1, 2, 3]`
 - **列表推导式**：`[x**2 for x in range(10) if x % 2 == 0]`

 ### 4.2 字符串（str）
 - **特点**：有序、不可变
 - **常用方法**：
   - `split(sep)`：分割成列表
   - `join(iterable)`：将可迭代对象连接成字符串
   - `replace(old, new)`：替换
   - `strip()` / `lstrip()` / `rstrip()`：去除两端空白
   - `find(sub)` / `index(sub)`：查找子串
   - `startswith(prefix)` / `endswith(suffix)`：判断首尾
   - `upper()` / `lower()`：大小写转换
   - `count(sub)`：统计出现次数

 ### 4.3 元组（tuple）
 - **特点**：有序、不可变、可重复
 - **创建**：`t = (1, 2, 3)` 或 `tuple()`
 - **元组解包**：`a, b, c = (1, 2, 3)`
 - **组包与解包**：函数返回多个值时自动组包，赋值时自动解包
 - **使用场景**：存储不应修改的数据，如坐标、配置项

 ### 4.4 集合（set）
 - **特点**：无序、可变、不重复（自动去重）
 - **创建**：`s = {1, 2, 3}` 或 `set()`
 - **常用操作**：
   - `add(x)`：添加元素
   - `remove(x)` / `discard(x)`：删除元素（discard 不报错）
   - `union(s2)` / `|`：并集
   - `intersection(s2)` / `&`：交集
   - `difference(s2)` / `-`：差集

 ### 4.5 字典（dict）
 - **特点**：键值对存储、键唯一、可变、无序（Python 3.7+ 有序）
 - **创建**：`d = {"name": "张三", "age": 25}` 或 `dict()`
 - **常用操作**：
   - `d[key]` / `d.get(key, default)`：获取值
   - `d[key] = value`：设置/修改值
   - `keys()` / `values()` / `items()`：获取键/值/键值对视图
   - `update(d2)`：合并字典
   - `pop(key)`：删除指定键值对
   - `in` 运算符：判断键是否存在

 ## 五、核心语法 — 函数 — P57~P70

 ### 5.1 函数基础
 - **定义**：`def 函数名(参数列表):`
 - **参数**：形式参数（形参）与实际参数（实参）
 - **返回值**：`return` 语句，可返回多个值（自动组包为元组）
 - **说明文档**：函数体第一行的字符串 `"""说明文档"""`
 - **嵌套调用**：函数内部调用其他函数
 - **类型提示**（P71-P72）：
   ```python
   def add(a: int, b: int) -> int:
       return a + b
   ```

 ### 5.2 函数进阶
 - **变量作用域**：
   - 局部变量：函数内部定义
   - 全局变量：模块顶层定义，函数内使用 `global` 声明
   - `nonlocal`：嵌套函数中修改外层变量
 - **传参方式**：
   - 位置参数：按顺序传参
   - 关键字参数：`func(name="张三", age=25)`
   - 默认参数：`def func(name, age=18)` — 默认值在定义时计算
   - 不定长参数：
     - `*args`：接收任意数量位置参数（元组）
     - `**kwargs`：接收任意数量关键字参数（字典）
 - **函数作为参数**：高阶函数，如 `map()`、`filter()`、`sorted(key=)`
 - **匿名函数（lambda）**：
   ```python
   lambda x, y: x + y
   # 等价于 def func(x, y): return x + y
   ```
 - **递归函数**：函数调用自身，必须有终止条件

 ## 六、核心语法 — 模块与包 — P73~P76

 ### 6.1 模块
 - **概念**：以 `.py` 文件形式存在的 Python 代码文件
 - **导入方式**：
   - `import 模块名`
   - `from 模块名 import 功能名`
   - `import 模块名 as 别名`
   - `from 模块名 import *`（不推荐）
 - **自定义模块**：创建 `.py` 文件，其中定义函数、类、变量
 - `if __name__ == "__main__":` — 模块作为脚本运行时执行

 ### 6.2 包（Package）
 - **概念**：包含 `__init__.py` 文件的文件夹，用于组织模块
 - `__init__.py`：包初始化文件，可定义 `__all__` 控制导入内容
 - **导入方式**：`from 包名 import 模块名`
 - **第三方包**：通过 `pip install 包名` 安装使用

 ## 七、核心语法 — 面向对象编程 — P77~P85

 ### 7.1 类与对象
 - **类**：对象的模板，使用 `class` 关键字定义
   ```python
   class Student:
       pass
   ```
 - **对象（实例）**：`stu = Student()` — 类的具体实例
 - **`__init__` 方法**：构造方法，创建对象时自动调用，用于初始化属性
 - **`self` 参数**：代表实例本身，是实例方法的第一个参数

 ### 7.2 实例方法与属性
 - **实例方法**：第一个参数为 `self`，通过对象调用
 - **实例属性**：在 `__init__` 中通过 `self.属性名 = 值` 定义
 - **类属性**：直接在类中定义，所有实例共享
   ```python
   class Student:
       school = "北京大学"    # 类属性

       def __init__(self, name):
           self.name = name  # 实例属性
   ```

 ### 7.3 魔法方法
 | 方法 | 用途 |
 |------|------|
 | `__init__(self)` | 构造方法 |
 | `__str__(self)` | 字符串表示，`print()` 时调用 |
 | `__repr__(self)` | 开发者友好表示 |
 | `__len__(self)` | `len()` 时调用 |
 | `__eq__(self, other)` | `==` 比较时调用 |
 | `__lt__(self, other)` | `<` 比较时调用 |

 ### 7.4 综合案例 — 教务系统
 前87集中包含一个完整的教务系统实战案例：
 - 学生信息的添加、修改、删除、查询功能
 - 面向对象设计，使用类组织数据和行为
 - 菜单驱动的交互式命令行界面
 - 数据存储在列表中

 ## 八、核心语法 — 异常处理 — P86~P87

 ### 8.1 异常捕获
 - `try...except...else...finally` 结构：
   ```python
   try:
       可能出错的代码
   except 异常类型 as e:
       处理异常的代码
   else:
       没有异常时执行
   finally:
       无论是否异常都执行
   ```
 - **常见异常类型**：`ValueError`、`TypeError`、`IndexError`、`KeyError`、`FileNotFoundError`、`ZeroDivisionError`
 - **捕获所有异常**：`except Exception as e:`（不推荐裸 `except:`）

 ### 8.2 异常抛出
 - `raise 异常类型("错误信息")` — 主动触发异常
 - **自定义异常**：继承 `Exception` 类

 ### 8.3 异常处理在案例中的应用
 - 在教务系统中输入数字时捕获 `ValueError`
 - 文件操作时处理 `FileNotFoundError`
 - 字典操作时处理 `KeyError`

 ## 九、总结与知识图谱

 ### 学习路径
 ```
 Python入门 → 变量与类型 → 运算符
     ↓
 流程控制（if/while/for）
     ↓
 数据容器（列表/元组/字符串/集合/字典）
     ↓
 函数（基础 → 进阶 → lambda → 递归）
     ↓
 类型注解 → 模块与包
     ↓
 面向对象编程（类/对象/魔法方法）
     ↓
 异常处理
 ```

 ### 核心编程范式
 - **面向过程**：函数式组织代码，按步骤执行
 - **面向对象**：类与对象组织代码，封装数据与行为
 - **模块化**：将代码拆分为模块和包，提高复用性

 ### 学习建议
 - 前87集完成 Python 基础语法，为后续 AI、Web 开发打下扎实基础
 - 重点掌握：数据结构（数据容器）、函数高级用法、面向对象思想
 - 建议每个知识点都动手写代码，配合实战案例加深理解
 - 从 P88 开始将进入 AI 相关的高级内容
